sudo: required

language: bash

services:
  - docker

before_install:
  - docker pull koalaman/shellcheck

script:
  - docker run koalaman/shellcheck --version
  - docker run -v $(pwd):/scripts koalaman/shellcheck /scripts/usr/local/modpd/bin/modpd
  - docker run -v $(pwd):/scripts koalaman/shellcheck /scripts/usr/local/modpd/etc/modpd.conf
  - docker run -v $(pwd):/scripts koalaman/shellcheck /scripts/etc/init.d/modpd

matrix:
  fast_finish: true

notifications:
  email: false
